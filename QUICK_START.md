# 🚀 Быстрый старт Event Manager

## Вариант 1: Полный Docker (рекомендуется)

### 1. Запуск одной командой:
```bash
make start
# или
./scripts/start.sh
# или
docker-compose up -d
```

### 2. Проверка статуса:
```bash
make status
# или
docker-compose ps
```

### 3. Тестирование:
```bash
make test
# или
./scripts/test-api.sh
```

## Вариант 2: Разработка

### 1. Запуск только базы данных:
```bash
make dev
# или
docker-compose -f docker-compose.dev.yml up -d
```

### 2. Установка зависимостей:
```bash
npm install
```

### 3. Запуск в режиме разработки:
```bash
npm run dev
```

## 🛠️ Полезные команды

```bash
# Просмотр всех команд
make help

# Логи приложения
make logs

# Логи базы данных
make logs-db

# Перезапуск
make restart

# Остановка
make stop

# Полная очистка
make clean
```

## 🔐 Тестирование API

### 1. Регистрация пользователя:
```bash
curl -X POST http://localhost:3000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест Тестов",
    "email": "test@example.com",
    "password": "TestPass123"
  }'
```

### 2. Вход в систему:
```bash
curl -X POST http://localhost:3000/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPass123"
  }'
```

### 3. Создание события (замените YOUR_TOKEN на полученный токен):
```bash
curl -X POST http://localhost:3000/api/events \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "title": "Тестовое событие",
    "description": "Описание события",
    "date": "2024-12-31T18:00:00.000Z"
  }'
```

## 📊 Мониторинг

- **API**: http://localhost:3000
- **База данных**: localhost:5432
- **Redis**: localhost:6379
- **Логи**: `logs/security-YYYY-MM-DD.log`

## 🆘 Решение проблем

### Проблема: Порты заняты
```bash
# Остановить все контейнеры
docker-compose down

# Проверить занятые порты
netstat -tulpn | grep :3000
netstat -tulpn | grep :5432
```

### Проблема: Ошибки базы данных
```bash
# Пересоздать базу данных
docker-compose down -v
docker-compose up -d
```

### Проблема: Ошибки приложения
```bash
# Просмотр логов
make logs

# Перезапуск приложения
make restart
```

## 🔒 Безопасность

Все настройки безопасности уже настроены:
- JWT аутентификация
- Bcrypt хеширование паролей
- Rate limiting
- XSS защита
- Логирование безопасности

Подробнее см. [SECURITY.md](SECURITY.md)
